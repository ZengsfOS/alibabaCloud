server:
  port: 8010

spring:
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        namespace: public

logging:
  level:
    com.zengsf.order.feign: debug

# feign日志局部配置
#feign:
#  client:
#    config:
#      stock-service:
#        logger-level: basic